# NDIS Action AI GUI

A simple voice-first web interface for interacting with an NDIS Action AI service.

## Development

```bash
npm install
npm start
```

Then open [http://localhost:3000](http://localhost:3000) in a browser with microphone access.
